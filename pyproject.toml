[project]
name = "kellox-basement"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "s04tneub", email = "s04tneub@uni-bonn.de" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
kellox-basement = "kellox_basement:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
